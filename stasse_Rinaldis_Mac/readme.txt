stasse_rinaldis/
├── Models/
│   └── Card.swift            → le modèle + données mock
├── ViewModels/
│   └── CardViewModel.swift   → logique + appel API
├── Views/
│   ├── CardListView.swift    → liste principale
│   ├── CardDetailView.swift  → stats au tap
│   └── CardRow.swift         → cellule de la liste
└── stasse_rinaldisApp.swift  → point d'entrée